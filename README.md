# Emergency_Dispatch_System

This website is developed using PHP. It cannot be fully hosted on GitHub because PHP requires a server-side environment, such as XAMPP, to execute scripts and process backend functions.
